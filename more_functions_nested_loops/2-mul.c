#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: First operand
 * @b: Second operand
 *
 * Return: multiplcation a * b
*/
int mul(int a, int b)
{
	int multipli;

	multipli = a * b;

	return (multipli);
}
